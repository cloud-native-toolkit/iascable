apiVersion: cloud.ibm.com/v1alpha1
kind: BillOfMaterial
metadata:
  name: existing-cluster
spec:
  modules:
    - name: ocp-login
    - name: openshift-cicd
    - name: console-link-job
    - name: cluster-config
      alias: config
  variables:
    - name: config_banner_text
